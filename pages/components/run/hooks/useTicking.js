import React from "react";
import Proptypes from "prop-types";

const useTicking = (initial) => {
  // 미완성: 시간 훅을 어떤 식으로 하는지(모범 케이스) 보고 나서 하는 게 좋을듯...
  // onStart, onStop 같은 걸 해야 할지도.
  // 현재 버튼 기준으로 뭔가 되어 있는데, onStart, onStop이... 여기에서 하는 게 더 직관적이고 나을 것 같다.
  return <div>useTicking</div>;
};

useTicking.prototype = {};

export default useTicking;
